## Place the following folders and files inside this `datasets/` directory:

- `ABIDE_packed/`
- `ADHD200_packed/`
- `ABIDE.csv`: metadata CSV with columns including `SUB_ID`, `SEX`, `AGE_AT_SCAN`.
- `ADHD.csv`: metadata CSV with columns including `ScanDir ID`, `Gender`, `Age`.

Optional:

- `AAL node.xls` or `AAL node.csv`

Notes
